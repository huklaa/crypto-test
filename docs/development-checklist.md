# Development checklist

Use this checklist before opening a pull request.

- Install dependencies with the repository's supported Node.js and pnpm versions.
- Run `pnpm run check` for syntax, unit, and contract checks.
- Run `pnpm run demo:build` when changing the Base portfolio demo.
- Keep secrets and private keys out of commits and examples.
- Update documentation when public behavior or commands change.
