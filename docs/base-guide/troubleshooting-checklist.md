# Troubleshooting checklist

When a Base workflow fails, check the basics in a fixed order: expected network, RPC connectivity, signer availability, account balance, contract address, ABI compatibility, input units, allowance requirements, gas estimation, and receipt status.

Capture the failing transaction hash or read call parameters before changing code. A consistent checklist prevents random retries and makes recurring problems easier to identify.
