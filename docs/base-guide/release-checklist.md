# Release checklist

Before publishing a new build, run tests, confirm the target network, review contract addresses, check environment variables, and verify that no secrets are included in the diff.

For contract changes, compare generated artifacts and deployment configuration. For application changes, exercise at least one complete read and write flow before tagging the release.
