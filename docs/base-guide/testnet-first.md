# Testnet-first workflow

Exercise new transaction flows in a non-production environment before using them with real assets. Verify network detection, contract addresses, permissions, error handling, and receipt parsing there first.

A successful test should cover both the happy path and at least one expected failure so the application proves it can surface errors clearly.
