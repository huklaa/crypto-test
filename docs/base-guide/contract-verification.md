# Contract verification

Keep compiler settings, source files, constructor arguments, and linked-library information reproducible so deployed bytecode can be verified later.

Verification should be part of the release checklist rather than an afterthought. Record the deployment transaction and build configuration together to make future audits and debugging easier.
