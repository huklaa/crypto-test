# Error messages

Translate low-level provider failures into messages that preserve the original cause while giving the user a useful next step. Keep the raw error available in logs for debugging.

Differentiate configuration errors, rejected signatures, contract reverts, RPC outages, and insufficient funds instead of displaying one generic transaction failure.
