# Deployment records

For each deployment, record the network, contract address, deployment transaction, source revision, and relevant constructor parameters. Keep these records separate from secrets and signer credentials.

A small deployment manifest makes it easier to reproduce a release, compare environments, and avoid accidentally using an address from a different network.
